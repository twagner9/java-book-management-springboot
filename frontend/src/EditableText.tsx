type EditableTextProps = {
    isBeingEdited: boolean,
    currentText: string,
    onUpdateText: () => void,
    className?: string,
};

export function EditableText({isBeingEdited, currentText, onUpdateText, className}: EditableTextProps) {

};